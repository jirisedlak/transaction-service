<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.3</version>
        <relativePath/>
    </parent>

    <groupId>com.assessment</groupId>
    <artifactId>transaction-service</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>transaction-service</name>
    <description>Spring Boot microservice exposing idempotent POST /transactions and POST /events endpoints</description>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <springdoc.version>2.8.6</springdoc.version>
        <!-- port the app is started on while the OpenAPI spec is generated during the build -->
        <openapi.port>18080</openapi.port>
        <!-- -DskipOpenApi=true skips starting the app and generating docs/openapi.yaml -->
        <skipOpenApi>false</skipOpenApi>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <!-- start the app around the integration-test phase so the spec can be fetched from it -->
                    <execution>
                        <id>start-for-openapi</id>
                        <goals>
                            <goal>start</goal>
                        </goals>
                        <configuration>
                            <skip>${skipOpenApi}</skip>
                            <arguments>
                                <argument>--server.port=${openapi.port}</argument>
                                <argument>--logging.level.root=WARN</argument>
                                <argument>--http.payload-logging.enabled=false</argument>
                            </arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>stop-for-openapi</id>
                        <goals>
                            <goal>stop</goal>
                        </goals>
                        <configuration>
                            <skip>${skipOpenApi}</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!-- generates docs/openapi.yaml from the running app (integration-test phase) -->
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-maven-plugin</artifactId>
                <version>1.4</version>
                <executions>
                    <execution>
                        <id>generate-openapi</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <skip>${skipOpenApi}</skip>
                    <apiDocsUrl>http://localhost:${openapi.port}/v3/api-docs.yaml</apiDocsUrl>
                    <outputDir>${project.basedir}/docs</outputDir>
                    <outputFileName>openapi.yaml</outputFileName>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
